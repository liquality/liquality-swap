import metamask from '../icons/metamask.svg'
import ledger from '../icons/ledger.svg'

const wallets = {
  'metamask': {
    icon: metamask
  },
  'ledger': {
    icon: ledger
  }
}

export default wallets
